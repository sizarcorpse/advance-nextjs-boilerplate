export const dynamic = "force-dynamic";
import Dialog from "@/components/Dialog";

const ShoutModal = () => {
  return (
    <main>
      <Dialog>
        <h1>Shout Modal</h1>
      </Dialog>
    </main>
  );
};

export default ShoutModal;
